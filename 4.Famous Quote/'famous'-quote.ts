// Storing the famous quote in a variable
let famous_quote: string = "Imagination is more important than knowledge.";

// Storing the author's name in a variable
let author_name: string = "Albert Einstein";

// Composing the message
let message: string = ` ${author_name} once said, "${famous_quote}"`;

// Printing the message
console.log(message);


